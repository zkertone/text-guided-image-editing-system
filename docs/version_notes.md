# 版本迭代记录

## V1.0

### 版本名称

V1.0 基础推理验证版本

### 本次更新内容

本版本完成了毕业设计项目的第一阶段技术可行性验证，初步实现了基于预训练扩散模型的文字驱动图像编辑流程。系统采用 Hugging Face Diffusers 框架调用 InstructPix2Pix 管线，在云端 GPU 环境中成功完成了模型加载、文本指令输入、图像推理生成以及结果输出的基本闭环。

在本阶段中，项目的重点并不在于构建完整界面或实现复杂功能，而是首先验证课题所设定技术路线的可行性，即：在不从零训练模型的前提下，能否基于现有预训练模型完成“输入图像 + 输入文本指令 → 输出编辑结果图像”的核心流程。实验结果表明，该技术路线是可行的，模型能够根据输入的文本语义对原始图像进行一定程度的内容修改与风格调整。

### 解决的问题或达到的阶段

该版本的完成，标志着项目已经从“课题方案与技术设想”正式推进到“可实际运行的原型验证”阶段，为后续用户自定义输入、界面封装、实验留档以及系统工程化整理奠定了基础。

### 下一步计划

在基础推理验证成功的前提下，进一步支持用户自定义图像输入与编辑指令测试，使系统从固定样例验证过渡到面向实际输入场景的功能验证。

## V1.1

### 版本名称

V1.1 用户自定义输入验证版本

### 本次更新内容

本版本在基础示例验证的基础上，进一步实现了用户自定义图像输入与文本指令编辑功能。系统已不再局限于固定测试样例，而是能够接收用户上传的真实图像，并根据输入的英文编辑指令生成对应的图像编辑结果。通过该版本的实现，项目完成了从“官方示例验证”到“用户输入验证”的过渡，说明文字驱动图像编辑流程已经具备基本实用性。

在本阶段实验中，系统完成了人物头发颜色修改、背景变化等初步测试，验证了预训练扩散模型在图像属性调整和场景语义变换方面的可行性。同时也观察到，当编辑目标较复杂时，模型可能对非目标区域产生附带影响，这为后续引入局部编辑与区域约束提供了依据。

### 解决的问题或达到的阶段

该版本的完成，标志着项目已经初步具备“输入图像—输入文本—输出结果”的基础交互能力，为后续工程化整理与界面封装奠定了基础。

### 下一步计划

将当前 notebook 中已验证成功的代码进行函数化封装，并逐步整理为更正式的工程结构，为 Gradio 界面接入和项目模块化做准备。

## V1.2

### 版本名称

V1.2 实验信息可视化与结果留档版本

### 本次更新内容

本版本在 V1.1 的基础上，进一步完善了系统的工程化表达能力与实验记录能力。除了保留原有的图像编辑功能外，系统新增了输入图像与输出图像的自动保存机制，并在 Gradio 页面中增加了实验信息展示区域，用于显示本次实验的输入图保存路径、输出图保存路径以及关键推理参数摘要。

通过这一改进，系统不再只是简单返回编辑后的图像结果，而是具备了基础的实验留档与结果追踪能力。这使得每次图像编辑操作都能够形成较为完整的记录，便于后续进行实验截图整理、参数对比分析以及论文撰写中的过程复现。

### 解决的问题或达到的阶段

该版本的实现，使项目从“可运行 Demo”进一步过渡到“具备基本系统特征的原型”，增强了项目在答辩展示中的完整性，也为后续实验管理和功能扩展提供了更清晰的基础。

### 下一步计划

继续增强系统的实验记录能力，使参数、提示词与结果图能够以结构化方式自动保存，形成可直接用于论文分析和结果整理的实验日志。

## V1.3 版本记录

### V1.3 实验日志结构化保存版本

本版本在 V1.2 的基础上，进一步增强了系统的实验记录能力，使每次图像编辑操作不仅能够保存输入图像和输出图像，还能够将关键实验信息以结构化形式追加记录到 `docs/experiment_log.csv` 文件中。记录内容包括文本指令、推理步数、图像引导强度、文本引导强度、图像尺寸以及输入输出图像路径等关键信息。

通过这一更新，系统的实验过程不再依赖手工整理和事后回忆，而是能够在每次推理完成后自动形成一条可追踪、可汇总、可分析的实验记录。这一功能显著提升了项目在实验管理、结果整理和论文写作阶段的效率，也使后续参数对比实验和案例归纳具备了更好的数据基础。

该版本的完成，标志着项目从“能够保存结果图像”进一步推进到“能够自动形成结构化实验日志”的阶段，使系统在工程化和研究记录层面更加完整，为毕业论文中的实验章节撰写提供了直接支持。

**下一步计划：**  
在现有实验日志自动保存功能的基础上，继续优化系统的交互体验与实验展示能力，例如补充更友好的界面提示、参数展示优化，或进一步推进局部编辑与 Inpainting 等后续功能的实现。

## V1.4 版本记录

### V1.4 界面交互优化与展示增强版本

本版本在 V1.3 的基础上，进一步优化了系统的界面交互体验与实验信息展示方式。系统新增了对空提示词输入的友好提示机制，避免用户在未输入编辑指令时直接触发生硬报错；同时在界面中补充了常用示例 prompt，降低了用户在初次使用时的操作门槛，使系统交互过程更加直观和清晰。

在本次更新中，实验信息展示区域也得到了进一步完善。系统在原有参数摘要与输入输出路径信息的基础上，新增了模型名称与运行设备信息，并对整体展示格式进行了统一整理，使实验结果页面能够更完整地反映当前系统配置与运行状态。这一改进不仅提升了界面的可读性，也增强了系统在实验截图整理、答辩展示和开发留档中的实用性。

### 解决的问题或达到的阶段

该版本的完成，标志着项目在现有文字驱动图像编辑功能基础上，进一步提升了交互友好性和展示完整性，使系统从“能够完成图像编辑任务”进一步推进到“能够更清晰地服务于实验展示与用户操作”的阶段，为后续继续扩展局部编辑和更强可控性功能奠定了良好基础。

### 下一步计划

在现有系统稳定运行的基础上，进一步尝试实现局部区域编辑能力，例如引入基于 Mask 的 Inpainting 方案，使系统逐步具备更精细的图像编辑控制能力。

## V1.5.1 版本记录

### V1.5.1 局部编辑效果优化与使用引导修正版

本版本在 V1.5 的基础上，进一步针对局部编辑模式的展示方式与使用体验进行了优化。系统修正了局部编辑模式下实验信息的参数展示逻辑，明确标注图像引导强度在当前 Inpainting 流程中未被使用，避免用户对参数作用产生误解，使实验信息展示与实际推理过程保持一致。

在本次更新中，界面中还补充了局部编辑的使用提示与更贴近实际场景的推荐 prompt 示例，使用户能够更清楚地理解黑白 mask 的含义、局部编辑的使用方式以及更适合的文本指令写法。这一改进降低了局部编辑功能的使用门槛，也提升了系统在答辩展示和实验截图整理中的可解释性与完整性。

### 解决的问题或达到的阶段

该版本的完成，标志着项目在实现局部编辑功能之后，进一步完善了局部编辑阶段的交互说明与信息表达方式，使系统从“具备局部编辑能力”进一步推进到“具备更清晰使用引导与展示逻辑”的阶段，为后续继续优化局部编辑效果和扩展系统功能奠定了更稳定的基础。

### 下一步计划

在现有整体编辑和局部编辑能力已经基本成型的基础上，进一步围绕系统功能增强与演示效果提升展开后续开发，例如补充更完善的实验案例、增强系统整理程度，或继续扩展新的编辑能力。

## V1.6 版本记录

### V1.6 在线 Mask 交互编辑增强版

本版本在 V1.5.1 的基础上，进一步增强了系统的局部编辑交互能力。在保留原有上传黑白 Mask 图方式的前提下，系统新增了在线绘制 Mask 的交互式编辑方式，使用户能够直接在界面中标注待编辑区域，并将绘制结果自动转换为标准黑白 Mask 后送入现有 Inpainting 流程。该版本使局部编辑不再完全依赖外部预先制作的 Mask 图，显著提升了系统在演示、实验和实际使用过程中的便捷性与交互性。

在本次更新中，系统还对实验信息摘要和 CSV 日志记录进行了同步扩展，新增了 Mask 来源字段，用于区分上传 Mask 与在线绘制两种局部编辑方式。这一改进使实验过程的记录更加完整，也增强了系统在实验留档、结果整理和答辩展示中的可解释性。

### 解决的问题或达到的阶段

该版本的完成，标志着项目在局部编辑方向上从“支持 Mask 输入”进一步推进到“支持交互式区域标注与编辑”的阶段，使系统更加贴近完整图像编辑原型的实现目标。

### 下一步计划

在现有整体编辑、上传 Mask 局部编辑和在线绘制 Mask 局部编辑功能已经基本成型的基础上，进一步围绕编辑效果优化、系统展示增强或后续功能扩展开展下一阶段开发。
